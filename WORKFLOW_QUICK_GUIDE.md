# 🚀 工作流快速指南

## 📋 分支说明

```
main  ← 镜像上游 (The-Agent-Builder/Code-reader master)
  ↓
dev   ← 您的日常开发分支 ⭐
```

**重要**: 永远不要在 `main` 分支直接开发!

---

## ⚡ 常用命令速查

### 每天开始工作

```bash
./sync-upstream.sh        # 同步上游到 main
./merge-main-to-dev.sh    # 合并到 dev
```

### 开发过程中

```bash
# 在 dev 分支编辑代码...
./push-changes.sh         # 提交并推送
```

### 准备提交 PR

```bash
./prepare-pr.sh           # 合并 dev 到 main
# 然后在 GitHub 上创建 PR
```

---

## 📊 完整工作流程

### 1️⃣ 开始新的一天

```bash
./sync-upstream.sh        # ✅ 同步上游最新代码
./merge-main-to-dev.sh    # ✅ 更新到 dev 分支
```

### 2️⃣ 日常开发

```bash
# 编辑代码...
git status                # 查看修改
./push-changes.sh         # 提交推送
```

### 3️⃣ 提交 PR 到上游

```bash
./prepare-pr.sh           # 准备 PR
```

然后访问: https://github.com/kay120/Code-reader
点击 "Contribute" → "Open pull request"

---

## 🎯 脚本功能一览

| 脚本 | 功能 | 使用频率 |
|------|------|----------|
| `sync-upstream.sh` | 同步上游到 main | 每天开始 |
| `merge-main-to-dev.sh` | 合并 main 到 dev | 同步后 |
| `push-changes.sh` | 提交推送到 dev | 开发时 |
| `prepare-pr.sh` | 准备 PR | 提交前 |
| `update-deps.sh` | 更新依赖 | 偶尔 |

---

## ⚠️ 注意事项

1. **始终在 dev 分支开发**
   - main 分支只用于镜像上游
   - 如果不小心在 main 开发,`push-changes.sh` 会提醒您

2. **定期同步上游**
   - 每天开始工作前运行 `./sync-upstream.sh`
   - 避免代码冲突

3. **提交前检查**
   - 运行 `git status` 查看修改
   - 确保没有不想提交的文件

---

## 🆘 遇到问题?

### 冲突了怎么办?

```bash
# 1. 查看冲突文件
git status

# 2. 手动编辑解决冲突

# 3. 标记为已解决
git add .
git commit -m "解决冲突"

# 4. 推送
git push origin dev
```

### 想放弃当前修改?

```bash
git restore .             # 放弃所有未提交的修改
```

### 查看当前分支?

```bash
git branch                # 查看所有分支,* 标记当前分支
```

---

## 📚 更多信息

详细文档请查看: [SCRIPTS_README.md](./SCRIPTS_README.md)

