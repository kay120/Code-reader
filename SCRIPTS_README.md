# 🛠️ 常用脚本说明

本目录包含了几个常用的 Shell 脚本,帮助您更方便地管理代码仓库。

## 📜 脚本列表

### 1. `sync-upstream.sh` - 同步上游代码

**用途**: 从上游仓库 (The-Agent-Builder/Code-reader) 同步最新代码到本地和您的 fork

**使用方法**:
```bash
./sync-upstream.sh
```

**执行步骤**:
1. 切换到 main 分支
2. 从上游仓库拉取最新代码
3. 推送到您的 fork (kay120/Code-reader)

**使用场景**:
- 每次开始工作前,先同步上游最新代码
- 定期同步,保持您的 fork 与上游一致

---

### 2. `push-changes.sh` - 推送修改

**用途**: 提交并推送您的修改到 fork

**使用方法**:
```bash
./push-changes.sh
```

**执行步骤**:
1. 检查是否有修改
2. 显示当前修改列表
3. 询问您输入提交信息
4. 添加所有修改
5. 提交修改
6. 推送到您的 fork

**使用场景**:
- 完成代码修改后,快速提交并推送

---

### 3. `update-deps.sh` - 更新依赖

**用途**: 更新 Python 和 Node.js 依赖到最新版本

**使用方法**:
```bash
./update-deps.sh
```

**执行步骤**:
1. 使用 `uv sync --upgrade` 更新 Python 依赖
2. 使用 `pnpm update` 更新前端依赖

**注意事项**:
- `uv.lock` 和 `pnpm-lock.yaml` 已被 `.gitignore` 忽略
- 这些锁文件不会被提交到仓库

---

## 🔄 典型工作流程

### 场景 1: 开始新的工作

```bash
# 1. 同步上游最新代码
./sync-upstream.sh

# 2. 进行您的修改
# ... 编辑代码 ...

# 3. 提交并推送
./push-changes.sh
```

### 场景 2: 更新依赖

```bash
# 1. 更新依赖
./update-deps.sh

# 2. 测试是否正常工作
# ... 运行测试 ...

# 3. 如果一切正常,提交其他修改(不包括锁文件)
./push-changes.sh
```

---

## 📝 Git 配置说明

当前仓库配置了两个远程仓库:

- **origin**: `https://github.com/kay120/Code-reader.git` (您的 fork,有写权限)
- **upstream**: `https://github.com/The-Agent-Builder/Code-reader.git` (上游仓库,只读)

### 手动操作命令

如果您想手动执行,可以使用以下命令:

```bash
# 同步上游
git checkout main
git pull upstream master
git push origin main

# 提交修改
git add .
git commit -m "您的提交信息"
git push origin main

# 更新依赖
uv sync --upgrade
cd frontend && pnpm update && cd ..
```

---

## ⚠️ 注意事项

1. **执行前确保脚本有执行权限**:
   ```bash
   chmod +x *.sh
   ```

2. **遇到冲突时**: 如果同步上游时遇到代码冲突,脚本会停止执行,您需要手动解决冲突

3. **网络问题**: 确保网络连接正常,能够访问 GitHub

4. **认证问题**: 确保已配置 Git 认证(SSH 密钥或 Personal Access Token)

---

## 🆘 常见问题

### Q: 执行脚本时提示 "Permission denied"
A: 运行 `chmod +x *.sh` 给脚本添加执行权限

### Q: 同步时出现冲突怎么办?
A: 手动解决冲突后,运行:
```bash
git add .
git commit -m "解决冲突"
git push origin main
```

### Q: 如何查看当前状态?
A: 运行 `git status` 查看当前修改状态

---

## 📚 更多资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub Fork 工作流](https://docs.github.com/en/get-started/quickstart/fork-a-repo)
- [项目主 README](./README.md)

